# 小雷版agent Skill 与执行逻辑排查报告

## 一、架构全景：5 层调用链

```
用户消息
  ↓
1. SkillDispatcher.match_skill()        [core/engine/skill_dispatcher.py]
   └─ LLM 意图分类 → 规则降级（萃取技能→多步检测→否定→意图映射→关键词评分）
  ↓
2. SingleStepHandler                      [core/handlers/single_step_handler.py]
   └─ skill=="chat" → 闲聊
   └─ skill in _COMPLEX_SKILLS → _route_to_multi_agent()
   └─ 其他 → CognitivePipeline.run()
  ↓
3. CognitivePipeline                      [core/handlers/cognitive_pipeline.py]
   └─ 上下文增强 → 反问检测 → run_react() → 反思 → (重试/升级)
  ↓
4. ReActCore.run_react()                 [core/multi_agent_v2/agents/react_core.py]
   └─ 10轮 × 每轮15步 LLM→工具循环
   └─ 中间件链（MiddlewareChain）处理每次工具调用
  ↓
5. ToolRegistry                            [core/multi_agent_v2/tools/tool_registry.py]
   └─ task tool → spawn_subagent()
   └─ orchestrate tool → orchestrate_subagents()
```

---

## 二、Skill 匹配问题（SkillDispatcher）

### 2.1 配置文件缺失

```
config/skill_keywords.yaml  ← 不存在！
```

实际加载路径是：
```python
CONFIG_DIR = PROJECT_ROOT / "config"
config_path = str(CONFIG_DIR / "skill_keywords.yaml")
```

文件不存在时，回退到 `FallbackSkillsConfig`，**只有 2 个技能**：
```python
core_skills = [
    ("chat", ["你好", "hello", "聊天"], 1),
    ("weather", ["天气", "temperature"], 5),
]
```

**这意味着：`analyze` skill 的 836 字符角色定义不是来自这个系统的 YAML 配置，而是来自 ORCA 运行时的动态注入。**

### 2.2 SkillDispatcher 和 ORCA 是两套独立的系统

| 维度 | 小雷版 agent | ORCA |
|------|-------------|------|
| Skill 配置 | `config/skill_keywords.yaml`（不存在） | ORCA 内部动态生成 |
| Skill 来源 | `config/skill_agent_map.yaml`（8个skill） | 5个来源目录 + GitHub 远程 |
| 匹配算法 | 多因子评分（strong_pattern + keyword + priority） | 关键词频率匹配 |
| 子代理触发 | LLM 自主决定是否调 `task`/`orchestrate` tool | ORCA middleware 控制 |

### 2.3 skill_agent_map.yaml 只有 8 个 skill

```yaml
skill_agent_map:
  project_analysis: project_analyzer
  data_analysis: data_analyst
  web_scraper: web_scraper_expert
  system_toolbox: system_operator
  weather: weather_expert
  translator: translator
  deep_thinking: deep_thinker
  creative_writing: creative
```

**缺失 `analyze` skill**——这就是为什么 ORCA 动态生成了 `analyze` 但小雷版 agent 里没有任何对应的 skill 定义和 prompt 模板。

---

## 三、子代理触发机制的问题

### 3.1 子代理工具已实现，但依赖 LLM 主动调用

子代理系统**完全就绪**：
- `task` tool → `spawn_subagent()` ✅
- `orchestrate` tool → `orchestrate_subagents()` ✅
- `AgentProfile.ANALYZE` profile 存在 ✅
- `prompts/agents/analyze.txt` 提示词存在 ✅
- `prompts/tools/task.txt` 使用说明存在 ✅

但触发依赖一个条件：**LLM 必须在 ReAct 循环中主动选择调用 `task` 或 `orchestrate` tool**。

### 3.2 analyze profile 的限制

```python
AgentProfile.ANALYZE: {
    "allowed": None,           # 不硬限制
    "disallowed": ["write_file", "edit_file", "execute_shell", "execute_python"],
    "system_hint": _builder.get_agent_prompt("analyze"),
}
```

ANALYZE profile **禁止写文件和执行 shell**，但允许调子代理（因为 `task`/`orchestrate` 不在 disallowed 列表里）。

### 3.3 关键问题：LLM 为什么没调 task/orchestrate？

看 `prompts/tools/task.txt` 的触发说明：
```
WHEN TO USE (强烈推荐):
- 复杂/多步任务、多文件代码库探索、项目分析、需要并行调研多个模块时，优先用 task 派子代理
```

**分析 "使用子代理来处理DeepSeek harness那个项目" 这个消息：**
- 它是一个**单一任务**，不是"多步"
- 没有明确的"并行"或"多个独立任务"语义
- LLM 认为单 agent 做 ReAct 分析就够了

所以 LLM **没有调用 `task` tool**，而是直接用单 agent 跑 ReAct 循环（search_files → read_file → execute_shell）。

---

## 四、执行流程混乱的根本原因

### 问题 1：两套 Skill 系统并存且不同步

```
ORCA 层面:
  "analyze" → 动态生成 836 字符角色定义 → 单 agent ReAct

小雷版 agent 层面:
  "analyze" → 不在 skill_agent_map.yaml → 无对应 prompt → 无子代理调度
```

两个系统的 skill 定义完全脱节。ORCA 说"用了 analyze skill"，但小雷版 agent 里的 `analyze` prompt 根本没有指导 LLM 去调用子代理。

### 问题 2：analyze.txt 没有子代理指令

```
prompts/agents/analyze.txt:
  "You are a code analyst. You deeply read, search, and analyze code..."
  "Do NOT edit or create any files. You are read-only."
```

**没有任何地方提到 `task` 或 `orchestrate` tool**。LLM 看到这个 prompt 后，理所当然地用 `read_file`/`grep`/`execute_shell` 来做分析，而不是派子代理。

对比 `task.txt`:
```
"WHEN TO USE (强烈推荐):
 - 复杂/多步任务、多文件代码库探索、项目分析...优先用 task 派子代理"
```

`task.txt` 说了要派子代理，但 `analyze.txt` 没说。

### 问题 3：SkillDispatcher 的 skill 列表不完整

`skill_agent_map.yaml` 里没有 `analyze`，导致：
1. LLM 分类时不知道有 `analyze` 这个 skill
2. 关键词评分时没有 `analyze` 的 keywords/priority
3. 最终匹配到了 ORCA 的动态 `analyze`，但小雷版 agent 没有对应的 prompt

### 问题 4：结果总结存在幻觉

session 结束时，总结说"子代理执行结果被截断"，但实际：
- 工具调用日志中没有 `task` 或 `orchestrate` 的调用记录
- `SubagentStart` hook 没有被触发
- 整个执行是单 agent ReAct

**这是 ORCA 的 Result Summarizer 在"圆谎"**——它检测到用户说了"子代理"，但实际执行中没有子代理，于是编造了"被截断"的理由。

---

## 五、修复方案

### 高优先级

**1. 给 `analyze.txt` 加子代理调度指令**

```diff
 # prompts/agents/analyze.txt
  You are a code analyst. You deeply read, search, and analyze code to produce structured, actionable findings.
+
+When the codebase is large or has multiple independent modules:
+- Use the `task` tool to spawn sub-agents for parallel analysis
+- Each sub-agent should focus on one module/directory
+- Aggregate results from all sub-agents into a structured report
+
  Your strengths:
  - Thorough code review — reading multiple files to understand architecture
```

**2. 将 `analyze` 加入 `skill_agent_map.yaml`**

```yaml
skill_agent_map:
  project_analysis: project_analyzer
  data_analysis: data_analyst
  web_scraper: web_scraper_expert
  system_toolbox: system_operator
  weather: weather_expert
  translator: translator
  deep_thinking: deep_thinker
  creative_writing: creative
  analyze: analyze    # ← 新增
```

**3. 创建 `config/skill_keywords.yaml`**

```yaml
skills:
  analyze:
    keywords: ["分析", "分析项目", "了解项目", "查看项目", "研究项目"]
    strong_patterns: ["use.*sub.*agent.*analyz", "analyze.*with.*sub.*agent"]
    priority: 8
    description: "分析项目代码结构和依赖关系"
  project_analysis:
    keywords: ["项目分析", "分析项目结构", "理解项目"]
    priority: 7
    description: "深度分析项目架构"
```

### 中优先级

**4. 修复 Result Summarizer 的幻觉问题**

在 session 结束前，验证：
- 如果用户请求中提到"子代理"
- 但工具调用日志中没有 `task`/`orchestrate` 调用
- 则总结时必须说明"单 agent 执行"而非编造"子代理被截断"

**5. 改进 SkillDispatcher 的 LLM 分类 prompt**

当前 prompt 只列出 skill 名称和关键词，应该同时列出每个 skill 的**触发条件**（特别是"何时应使用子代理"的说明）。

### 低优先级

**6. 统一 ORCA 和 小雷版 agent 的 skill 定义**

要么让 ORCA 读取小雷版 agent 的 `skill_agent_map.yaml`，要么让两者的 skill 定义保持一致。

**7. 添加 skill 可用性自检**

在 session 启动时，检查 `skill_agent_map.yaml` 中列出的 skill 是否有对应的 `prompts/agents/<name>.txt` 文件，缺失则 warning。

---

## 六、对比：codemap 为什么成功触发了子代理

```
用户说: "生成 codemap"
  → SkillDispatcher 匹配到 codemap
  → codemap SKILL.md 明确写: "Spawn one fixer per folder"
  → LLM 调用 task/orchestrate tool
  → spawn_subagent() 执行
  → 多个子代理并行工作
```

而 `analyze` 的问题是：
```
用户说: "使用子代理来分析项目"
  → ORCA 匹配到动态生成的 analyze skill
  → 小雷版 agent 的 analyze.txt 没有子代理指令
  → LLM 用单 agent ReAct 完成分析
  → 总结时编造"子代理被截断"
```

---

## 七、关键文件索引

| 文件 | 作用 | 问题 |
|------|------|------|
| `config/skill_keywords.yaml` | Skill 关键词配置 | **不存在** |
| `config/skill_agent_map.yaml` | Skill→Agent 映射 | 缺少 `analyze` |
| `prompts/agents/analyze.txt` | analyze profile 提示词 | 无子代理指令 |
| `prompts/tools/task.txt` | task tool 使用说明 | 有子代理指令，但 analyze 没引用 |
| `core/engine/skill_dispatcher.py` | Skill 匹配算法 | 依赖不存在的 YAML |
| `core/multi_agent_v2/tools/tool_registry.py` | task/orchestrate 工具实现 | 实现正确 |
| `core/multi_agent_v2/agents/subagent/spawn.py` | 子代理生成逻辑 | 实现正确 |
| `core/multi_agent_v2/tools/toolsets.py` | 工具分组 | task/orchestrate 在 subagent 组 |
