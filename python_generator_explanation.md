# Python 生成器用途说明

## 核心用途
Python生成器的用途是：**通过`yield`关键字惰性逐个产出值，避免一次性将所有数据加载到内存，从而实现对大数据集或无限序列的高效迭代。**

## 详细说明

### 1. 惰性求值（Lazy Evaluation）
生成器按需生成值，而不是预先计算并存储所有结果。每次调用`next()`时，生成器从上次暂停的位置继续执行，直到遇到下一个`yield`。

### 2. 内存效率
对于大数据集，生成器可以将内存占用从O(n)降低到O(1)，因为不需要在内存中存储整个序列。

### 3. 支持无限序列
生成器可以表示理论上的无限序列，如斐波那契数列、自然数序列等，因为每次只生成一个值。

### 4. 简化代码
相比手动实现迭代器类，生成器使用`yield`语法更简洁，代码可读性更强。

## 示例代码

```python
# 生成器函数：生成1到n的平方
def square_numbers(n):
    for i in range(n):
        yield i ** 2

# 使用生成器
squares = square_numbers(5)
for num in squares:
    print(num)  # 依次输出: 0, 1, 4, 9, 16
```

## 应用场景
- 处理大型文件（逐行读取而不加载整个文件）
- 数据流处理（管道式数据处理）
- 生产者-消费者模式
- 异步编程（配合async/await）
- 状态机实现
