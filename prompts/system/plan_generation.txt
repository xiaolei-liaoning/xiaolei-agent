{role_description}
Analyze the task and create a concise execution plan that directly serves it.

Task: {task_description}
Available tools: {tool_list}

Rules:
- Each step must DIRECTLY advance the task above. Never invent unrelated work.
- Describe WHAT to accomplish, not HOW. The agent knows its tools.
- Keep it SHORT: 2-3 steps max.
- Start with getting information, end with producing the deliverable the task asks for.
- Every step should be one sentence of plain text.

Output format: one plain text line per step. No tool names.
Only output the plan steps, nothing else.
